name: Auto Sync Longhua Assets

on:
  schedule:
    - cron: '0 19 * * *'
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: 🧩 Checkout
        uses: actions/checkout@v4

      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.9'

      - name: 📦 Dependencies
        run: pip install requests

      - name: 🚀 Setup Proxy
        run: |
          wget -qO- https://github.com/MetaCubeX/mihomo/releases/download/v1.18.9/mihomo-linux-amd64-v1.18.9.gz | gunzip > clash
          chmod +x clash
          cat > config.yaml <<EOF
mixed-port: 7890
mode: global
proxies:
  - {name: "TW", type: ss, server: brtlee3ogRTfxtWt.xf9pzeslxw.sbs, port: 17001, cipher: aes-256-gcm, password: 1d23e3c5-c047-46a0-b7d8-d838aff7ae8d}
rules:
  - MATCH,TW
EOF
          ./clash -f config.yaml > clash.log 2>&1 &
          sleep 30

      - name: 🌐 Verify Proxy
        run: |
          # 增加重试逻辑测试代理
          for i in {1..3}; do
            RES=$(curl -s -m 10 -x http://127.0.0.1:7890 http://ip-api.com/json/)
            if [[ "$RES" == *"TW"* ]]; then
              echo "✅ 代理验证通过"
              exit 0
            fi
            echo "等待代理响应... $i"
            sleep 10
          done
          echo "❌ 代理无法连接台湾，请检查节点"
          exit 1

      - name: 🔄 Sync
        env:
          HTTP_PROXY: http://127.0.0.1:7890
          HTTPS_PROXY: http://127.0.0.1:7890
        run: python cloud_sync.py

      - name: 📤 Push
        run: |
          git config --local user.name "github-actions[bot]"
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git add workers.js
          git commit -m "update keys" || exit 0
          git push
