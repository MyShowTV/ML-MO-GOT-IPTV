name: 龙华频道 AssetID 自动同步

on:
  schedule:
    - cron: '0 0,12 * * *'
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 设置 Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: 安装依赖
        run: |
          pip install selenium webdriver-manager
          sudo apt-get update
          sudo apt-get install -y shadowsocks-libev

      - name: 启动 Shadowsocks 隧道
        run: |
          # 启动后台进程
          ss-local -s 154.223.20.190 -p 8388 -k "${{ secrets.SS_PASSWORD }}" -m aes-256-gcm -l 10808 &
          
          echo "等待隧道建立并测试连接..."
          # 循环测试，直到代理真正可用
          for i in {1..10}; do
            if curl -x socks5://127.0.0.1:10808 -I https://www.google.com --connect-timeout 5; then
              echo "✅ Shadowsocks 隧道已就绪"
              break
            fi
            echo "尝试中 ($i/10)..."
            sleep 5
          done

      - name: 执行抓取
        env:
          # 这里的变量会被 Python 脚本中的 setup_driver 读取
          HTTPS_PROXY: http://127.0.0.1:10808
          HTTP_PROXY: http://127.0.0.1:10808
        run: python longhua_sync.py

      - name: 推送更新
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          if [[ -n "$(git status --porcelain workers.js)" ]]; then
            git add workers.js
            git commit -m "🤖 自动更新 [$(date '+%Y-%m-%d %H:%M')]"
            git push
          else
            echo "无变动"
          fi
