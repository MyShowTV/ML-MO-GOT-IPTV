name: Ofiii-Auto-Capture

on:
  schedule:
    - cron: '0 */6 * * *' # 每 6 小时自动执行
  workflow_dispatch:      # 支持手动运行

jobs:
  build-and-run:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: 检出代码
        uses: actions/checkout@v3

      - name: 启动 Mihomo 并执行抓取
        run: |
          echo "===================== 🚀 初始化环境 ====================="
          sudo apt-get update
          sudo apt-get install -y wget curl python3 python3-pip google-chrome-stable
          
          echo "===================== ⚙️ 安装 Mihomo ====================="
          wget https://github.com/MetaCubeX/Mihomo/releases/download/v1.18.9/mihomo-linux-amd64-v1.18.9.gz
          gunzip mihomo-linux-amd64-v1.18.9.gz
          mv mihomo-linux-amd64-v1.18.9 mihomo
          chmod +x mihomo

          echo "===================== 🛰️ 启动台湾代理 ====================="
          ./mihomo -f clash_config.yaml -d . &
          sleep 15

          echo "===================== 🌏 验证代理出口 IP ====================="
          curl -x http://127.0.0.1:7890 https://ifconfig.me || echo "代理连接失败！"

          echo "===================== 💾 安装依赖 ====================="
          pip install -r requirements.txt

          echo "===================== 🔧 设置系统代理环境变量 ====================="
          export http_proxy=http://127.0.0.1:7890
          export https_proxy=http://127.0.0.1:7890

          echo "===================== 🧠 启动爬虫抓取密钥 ====================="
          python3 cloud_sync.py

      - name: 提交并推送更新
        run: |
          git config --local user.email "bot@github.com"
          git config --local user.name "TV-Bot"
          git add workers.js
          git commit -m "Update Channels: $(date)" || echo "没有变更，无需提交。"
          git push
