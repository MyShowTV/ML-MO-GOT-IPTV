name: Auto Sync Longhua Assets

on:
  schedule:
    # 每天凌晨3点运行（UTC时间）
    - cron: '0 19 * * *'  # 北京时间第二天凌晨3点
  workflow_dispatch:       # 支持手动触发
  push:
    branches: [ main ]
    paths:
      - 'cloud_sync.py'

env:
  PYTHON_VERSION: '3.9'
  CLASH_VERSION: 'v1.19.0'

jobs:
  sync-longhua:
    runs-on: ubuntu-latest
    
    steps:
      - name: 🧩 Checkout Code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          persist-credentials: true

      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: 📦 Install Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y jq wget curl unzip
          pip install --upgrade pip
          pip install selenium==4.9.1 requests==2.31.0

      - name: 🚀 Setup Clash Proxy
        run: |
          echo "设置 Clash 代理..."
          echo "正在下载 Clash 核心..."
          
          # 尝试多个版本（适应新的命名规则）
          CLASH_VERSIONS=("v1.19.0" "v1.18.5" "v1.18.0" "v1.17.0")
          DOWNLOAD_SUCCESS=false
          
          for CLASH_VER in "${CLASH_VERSIONS[@]}"; do
            echo "尝试下载版本: $CLASH_VER"
            
            # 尝试官方下载链接
            if wget -q "https://github.com/Dreamacro/clash/releases/download/$CLASH_VER/clash-linux-amd64-$CLASH_VER.gz"; then
              echo "✅ 下载成功: $CLASH_VER"
              gunzip "clash-linux-amd64-$CLASH_VER.gz"
              mv "clash-linux-amd64-$CLASH_VER" clash
              chmod +x clash
              DOWNLOAD_SUCCESS=true
              echo "Clash 版本: $CLASH_VER"
              break
            else
              echo "❌ 版本 $CLASH_VER 下载失败"
            fi
          done
          
          # 如果官方下载都失败，尝试备用链接
          if [ "$DOWNLOAD_SUCCESS" = false ]; then
            echo "尝试使用备用下载源..."
            
            # 方法1: 尝试直接下载最新版本
            wget -q https://github.com/Dreamacro/clash/releases/latest/download/clash-linux-amd64-v3.gz
            if [ $? -eq 0 ]; then
              gunzip clash-linux-amd64-v3.gz
              mv clash-linux-amd64-v3 clash
              chmod +x clash
              echo "✅ 使用备用链接下载成功"
              DOWNLOAD_SUCCESS=true
            else
              # 方法2: 使用 MIHOMO (Clash Meta) 作为替代
              echo "尝试下载 MIHOMO (Clash Meta)..."
              wget -q https://github.com/MetaCubeX/mihomo/releases/latest/download/mihomo-linux-amd64-v1.18.9.gz
              if [ $? -eq 0 ]; then
                gunzip mihomo-linux-amd64-v1.18.9.gz
                mv mihomo-linux-amd64-v1.18.9 clash
                chmod +x clash
                echo "✅ 使用 MIHOMO 替代成功"
                DOWNLOAD_SUCCESS=true
              fi
            fi
          fi
          
          if [ "$DOWNLOAD_SUCCESS" = false ]; then
            echo "❌ 所有下载尝试都失败了"
            echo "跳过代理设置，直接尝试访问..."
            echo "NO_PROXY=true" >> $GITHUB_ENV
            exit 0
          fi
          
          # 创建代理配置文件（台湾节点）
          echo "创建 Clash 配置文件..."
          cat > clash_config.yaml <<'EOF'
          mixed-port: 7890
          allow-lan: false
          mode: global
          log-level: info
          
          proxies:
            - name: "TW-01"
              type: ss
              server: brtlee3ogRTfxtWt.xf9pzeslxw.sbs
              port: 17001
              cipher: aes-256-gcm
              password: 1d23e3c5-c047-46a0-b7d8-d838aff7ae8d
              udp: false
            
            - name: "TW-02"
              type: ss
              server: brtlee3ogRTfxtWt.xf9pzeslxw.sbs
              port: 17002
              cipher: aes-256-gcm
              password: 1d23e3c5-c047-46a0-b7d8-d838aff7ae8d
              udp: false
            
            - name: "TW-03"
              type: ss
              server: brtlee3ogRTfxtWt.xf9pzeslxw.sbs
              port: 17003
              cipher: aes-256-gcm
              password: 1d23e3c5-c047-46a0-b7d8-d838aff7ae8d
              udp: false
          
          proxy-groups:
            - name: "PROXY"
              type: select
              proxies:
                - TW-01
                - TW-02
                - TW-03
          
          rules:
            - MATCH,PROXY
          EOF
          
          # 启动 Clash
          echo "启动 Clash 代理..."
          ./clash -f clash_config.yaml > clash.log 2>&1 &
          CLASH_PID=$!
          echo "CLASH_PID=$CLASH_PID" >> $GITHUB_ENV
          sleep 10
          
          # 验证代理连接
          echo "测试代理连接..."
          PROXY_SUCCESS=false
          for i in {1..5}; do
            echo "尝试 $i..."
            RESPONSE=$(curl -s --max-time 10 -x http://127.0.0.1:7890 "http://ip-api.com/json/?fields=countryCode,query" || echo '{"countryCode":"ERROR"}')
            COUNTRY=$(echo $RESPONSE | jq -r '.countryCode' 2>/dev/null || echo "ERROR")
            
            if [[ "$COUNTRY" == "TW" ]]; then
              IP=$(echo $RESPONSE | jq -r '.query')
              echo "✅ 代理连接成功 | IP: $IP | 国家: 台湾"
              PROXY_SUCCESS=true
              echo "PROXY_SUCCESS=true" >> $GITHUB_ENV
              break
            elif [[ "$COUNTRY" != "ERROR" ]]; then
              echo "尝试 $i: 当前 IP 国家: $COUNTRY"
            fi
            sleep 3
          done
          
          if [ "$PROXY_SUCCESS" = false ]; then
            echo "⚠️ 代理连接测试未通过台湾 IP"
            echo "PROXY_SUCCESS=false" >> $GITHUB_ENV
          fi

      - name: 🔄 Run Sync Script
        run: |
          echo "设置代理环境..."
          
          # 检查代理是否可用
          if [ -n "$CLASH_PID" ] && [ "$PROXY_SUCCESS" = "true" ]; then
            export http_proxy="http://127.0.0.1:7890"
            export https_proxy="http://127.0.0.1:7890"
            export HTTP_PROXY="http://127.0.0.1:7890"
            export HTTPS_PROXY="http://127.0.0.1:7890"
            echo "使用代理: http://127.0.0.1:7890"
            
            # 验证代理连接
            echo "验证网络环境..."
            RESPONSE=$(curl -s http://ip-api.com/json/ || echo '{"status":"fail"}')
            echo "当前网络: $RESPONSE"
          else
            echo "⚠️ 无代理模式运行"
            echo "注意：可能需要台湾 IP 才能访问龙华网站"
          fi
          
          echo "开始执行同步脚本..."
          python cloud_sync.py
          
          SYNC_EXIT_CODE=$?
          if [[ $SYNC_EXIT_CODE -eq 0 ]]; then
            echo "✅ 同步脚本执行成功"
          else
            echo "❌ 同步脚本执行失败，退出码: $SYNC_EXIT_CODE"
            exit $SYNC_EXIT_CODE
          fi

      - name: 📤 Commit Updates
        if: success()
        run: |
          echo "检查文件变更..."
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          
          # 检查是否有 JSON 结果文件
          if ls longhua_assets_*.json 1> /dev/null 2>&1; then
            echo "发现新的 AssetID 文件"
            git add longhua_assets_*.json
          fi
          
          # 检查 workers.js 是否被更新
          if [[ -f "workers.js" ]] && [[ -n $(git diff --name-only HEAD workers.js 2>/dev/null || echo "") ]]; then
            echo "workers.js 已被更新"
            git add workers.js
          fi
          
          # 检查日志文件
          if [[ -f "sync.log" ]]; then
            echo "添加同步日志"
            git add sync.log
          fi
          
          if [[ -n $(git status --porcelain) ]]; then
            git commit -m "🔄 Auto Sync Longhua Assets [$(date +'%Y-%m-%d %H:%M:%S')]"
            git push
            echo "📤 已提交更新"
          else
            echo "📭 没有需要更新的内容"
          fi

      - name: 🧹 Cleanup
        if: always()
        run: |
          echo "清理进程..."
          # 停止 Clash 进程
          if [ -n "$CLASH_PID" ]; then
            echo "停止 Clash 进程: $CLASH_PID"
            kill $CLASH_PID 2>/dev/null || true
          fi
          
          # 清理临时文件
          rm -f clash clash_config.yaml clash.log 2>/dev/null || true
          
          echo "工作流执行完成"

      - name: 📊 Upload Logs on Failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: error-logs-${{ github.run_id }}
          path: |
            clash.log
            sync.log
          retention-days: 3
