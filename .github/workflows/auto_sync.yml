name: Auto Sync Longhua Assets

on:
  schedule:
    - cron: '0 19 * * *' # 北京时间凌晨3点
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: 🧩 检出代码
        uses: actions/checkout@v4

      - name: 🐍 安装 Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.9'

      - name: 📦 安装依赖
        run: pip install requests

      - name: 🚀 启动代理
        run: |
          wget -q https://github.com/MetaCubeX/mihomo/releases/download/v1.18.9/mihomo-linux-amd64-v1.18.9.gz
          gunzip -f mihomo-linux-amd64-v1.18.9.gz
          mv mihomo-linux-amd64-v1.18.9 clash
          chmod +x clash
          nohup ./clash -f clash_config.yaml > clash.log 2>&1 &
          sleep 20
          # 验证代理是否生效
          curl -s -x http://127.0.0.1:7890 http://ip-api.com/json/ | grep "TW" || (echo "代理未指向台湾" && exit 1)

      - name: 🔄 执行同步
        env:
          HTTP_PROXY: http://127.0.0.1:7890
          HTTPS_PROXY: http://127.0.0.1:7890
        run: python cloud_sync.py

      - name: 📤 提交更新
        run: |
          git config --local user.email "actions@github.com"
          git config --local user.name "Github Action"
          git add workers.js
          git commit -m "🔄 自动更新钥匙 [$(date +'%Y-%m-%d %H:%M')]" || exit 0
          git push
