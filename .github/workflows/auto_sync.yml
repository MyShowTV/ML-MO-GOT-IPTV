name: Auto Sync Longhua Assets

on:
  schedule:
    - cron: '0 19 * * *'
  workflow_dispatch:
  push:
    branches: [ main ]
    paths:
      - 'cloud_sync.py'

env:
  PYTHON_VERSION: '3.9'

jobs:
  sync-longhua:
    runs-on: ubuntu-latest

    steps:
      - name: 🧩 Checkout Code
        uses: actions/checkout@v4
        with:
          persist-credentials: true

      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: 📦 Install Dependencies
        run: |
          sudo apt-get update -y
          sudo apt-get install -y jq curl wget unzip
          python -m pip install --upgrade pip
          pip install requests==2.31.0

      - name: 🚀 Setup Proxy (Mihomo)
        run: |
          echo "=== [阶段1] 下载 Mihomo 内核 ==="
          wget -q https://github.com/MetaCubeX/mihomo/releases/download/v1.18.9/mihomo-linux-amd64-v1.18.9.gz
          gunzip -f mihomo-linux-amd64-v1.18.9.gz
          mv mihomo-linux-amd64-v1.18.9 clash
          chmod +x clash

          echo "=== [阶段2] 创建代理配置文件 ==="
          cat > clash_config.yaml <<'EOF'
mixed-port: 7890
allow-lan: false
mode: global
log-level: info
proxies:
  - name: "TW-01"
    type: ss
    server: brtlee3ogRTfxtWt.xf9pzeslxw.sbs
    port: 17001
    cipher: aes-256-gcm
    password: 1d23e3c5-c047-46a0-b7d8-d838aff7ae8d
  - name: "TW-02"
    type: ss
    server: brtlee3ogRTfxtWt.xf9pzeslxw.sbs
    port: 17002
    cipher: aes-256-gcm
    password: 1d23e3c5-c047-46a0-b7d8-d838aff7ae8d
rules:
  - MATCH,DIRECT
EOF

          echo "=== [阶段3] 启动 Mihomo ==="
          nohup ./clash -f clash_config.yaml > clash.log 2>&1 &
          sleep 15

          echo "=== [阶段4] 测试出口 IP ==="
          PROXY_OK=false
          for i in {1..6}; do
            RES=$(curl -s --max-time 10 -x http://127.0.0.1:7890 "http://ip-api.com/json/?fields=countryCode,status" || echo '{"status":"fail"}')
            if [[ "$RES" == *"TW"* ]]; then
              echo "✅ 代理就绪: $RES"
              PROXY_OK=true
              echo "PROXY_SUCCESS=true" >> $GITHUB_ENV
              break
            fi
            sleep 5
          done

      - name: 🔄 Run Sync Script
        run: |
          if [[ "$PROXY_SUCCESS" == "true" ]]; then
            export http_proxy="http://127.0.0.1:7890"
            export https_proxy="http://127.0.0.1:7890"
          fi
          python cloud_sync.py

      - name: 📤 Commit Updates
        run: |
          git config --local user.email "actions@github.com"
          git config --local user.name "github-actions"
          git add workers.js
          git commit -m "🔄 Auto Sync [$(date +'%Y-%m-%d %H:%M:%S')]" || exit 0
          git push

      - name: 🧹 Cleanup
        if: always()
        run: |
          pkill -f clash || true
          rm -f clash clash_config.yaml clash.log
