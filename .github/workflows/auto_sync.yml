name: Auto Sync Longhua Assets

on:
  schedule:
    - cron: '0 19 * * *'
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: 🧩 检出代码
        uses: actions/checkout@v4

      - name: 🐍 安装 Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.9'

      - name: 📦 安装依赖
        run: pip install requests

      - name: 🚀 启动代理 (增强稳定性版)
        run: |
          wget -q https://github.com/MetaCubeX/mihomo/releases/download/v1.18.9/mihomo-linux-amd64-v1.18.9.gz
          gunzip -f mihomo-linux-amd64-v1.18.9.gz
          mv mihomo-linux-amd64-v1.18.9 clash
          chmod +x clash
          
          # 启动并在后台运行
          nohup ./clash -f clash_config.yaml > clash.log 2>&1 &
          
          echo "等待代理初始化 (30秒)..."
          sleep 30
          
          # 简单的连通性测试，不再强制要求必须含 "TW" 字符串
          echo "=== 代理测试结果 ==="
          curl -I -s -x http://127.0.0.1:7890 https://www.google.com || echo "⚠️ 代理可能未完全就绪"
          
      - name: 🔄 执行同步 (强制环境变量)
        env:
          # 同时设置大写和小写，确保 Python requests 必走代理
          http_proxy: http://127.0.0.1:7890
          https_proxy: http://127.0.0.1:7890
          HTTP_PROXY: http://127.0.0.1:7890
          HTTPS_PROXY: http://127.0.0.1:7890
        run: |
          # 如果脚本还是抓不到，至少打印出当时的 IP 信息方便排查
          echo "当前执行脚本的出口 IP 简报:"
          curl -s -x http://127.0.0.1:7890 http://ip-api.com/json/ || echo "无法获取IP信息"
          python cloud_sync.py

      - name: 📤 提交更新
        if: success()
        run: |
          git config --local user.email "actions@github.com"
          git config --local user.name "Github Action"
          git add workers.js
          git commit -m "🔄 自动同步钥匙 [$(date +'%Y-%m-%d %H:%M')]" || exit 0
          git push
